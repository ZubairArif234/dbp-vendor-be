/**
 * ─── External Service Clients ────────────────────────────────────────────────
 *
 * Initialise third-party SDK clients here so they are created once and shared.
 * Import the clients you need in services/controllers.
 *
 * Example (uncomment & install packages when ready):
 *
 *   import OpenAI from "openai";
 *   import twilio from "twilio";
 *   import { OPENAI_API_KEY, TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN } from "./env.js";
 *
 *   export const openai = OPENAI_API_KEY
 *     ? new OpenAI({ apiKey: OPENAI_API_KEY })
 *     : null;
 *
 *   export const twilioClient =
 *     TWILIO_ACCOUNT_SID && TWILIO_AUTH_TOKEN
 *       ? twilio(TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN)
 *       : null;
 */

// Placeholder – add your own client exports above
export {};
